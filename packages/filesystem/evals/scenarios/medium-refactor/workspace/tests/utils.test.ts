// Test file - should NOT be modified even though it has console.log
import { describe, it } from 'vitest';

describe('Utils', () => {
  it('should work', () => {
    console.log('Running test');
    // Test assertions
  });
});
